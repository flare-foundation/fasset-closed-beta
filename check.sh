check if this repo is latest
check if fasset-bots repo is correct `develop` and latest
check if config.json, .env and .coston.json files are the same
check if `user-bot agent --all`  returns only 1 and correct vault address agent - this will param
